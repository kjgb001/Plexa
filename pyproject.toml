[project]
name = "plexa"
version = "0.0.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["plexa_server"]

[tool.pytest.ini_options]
markers = [
    "postgres_only: test requires the postgres storage backend",
]
